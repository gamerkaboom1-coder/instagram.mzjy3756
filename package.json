{
  "name": "location-tracker-permanent",
  "version": "1.0.0",
  "description": "Instagram Location Tracker",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "tunnel:cf": "npx cloudflared tunnel --url http://localhost:3000",
    "tunnel:ngrok": "npx ngrok http 3000"
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^2.6.7",
    "dotenv": "^16.0.3"
  }
}
